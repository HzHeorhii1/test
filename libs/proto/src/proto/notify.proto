syntax = "proto3";

package notify;

service NotifyService {
  rpc SendNotification (SendNotificationRequest) returns (SendNotificationResponse);
}

message SendNotificationRequest  { string user_id = 1; string channel = 2; string message = 3; }
message SendNotificationResponse { string notification_id = 1; bool sent = 2; }
